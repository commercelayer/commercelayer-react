import {Config,Price} from './'

describe('Config', () => {
  it('is truthy', () => {
    expect(Config).toBeTruthy()
  })
})

describe('Price', () => {
  it('is truthy', () => {
    expect(Price).toBeTruthy()
  })
})
